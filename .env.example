# Happy Server Self-Hosting Configuration
# Copy this file to .env and update the values

# Required: Master secret for encryption (generate a strong random string)
HANDY_MASTER_SECRET=change-this-to-a-secure-random-string

# Optional: Custom port for the server (default: 3005)
# PORT=3005

# MinIO credentials (change these for production!)
MINIO_ROOT_USER=happyminio
MINIO_ROOT_PASSWORD=happyminiopassword

# Public URL for file access
# For production: https://your-domain.com/files
S3_PUBLIC_URL=http://localhost:3005/files

# Optional: Enable metrics endpoint
METRICS_ENABLED=false

# Optional: 11Labs API key for voice features
# ELEVEN_LABS_API_KEY=your-api-key

# Optional: GitHub OAuth (for GitHub integration features)
# GITHUB_CLIENT_ID=your-client-id
# GITHUB_CLIENT_SECRET=your-client-secret
